type Props = {};

const Button = (props: Props) => {
  return (
    <button className="text-3xl uppercase  hover:opacity-80 ease-in duration-200 bg-red-950 border-red text-white">
      Button test ssd
    </button>
  );
};

export default Button;
